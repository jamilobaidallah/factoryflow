# نظام إدارة المصنع - FactoryFlow 🏭

نظام ويب متكامل لإدارة العمليات المالية والمخزون للمصانع، مبني بأحدث التقنيات.

## 🚀 المميزات الرئيسية

- ✅ **إدارة العملاء**: تسجيل وتتبع بيانات العملاء وأرصدتهم
- ✅ **دفتر الأستاذ**: تسجيل جميع الحركات المالية (دخل/مصروفات)
- ✅ **المدفوعات**: تتبع عمليات القبض والصرف
- ✅ **الشيكات**: إدارة الشيكات الواردة والصادرة مع رفع الصور
- ✅ **المخزون**: تتبع حركة المواد (دخول/خروج)
- ✅ **لوحة تحكم**: إحصائيات ورسوم بيانية شاملة
- ✅ **التقارير**: تصدير البيانات إلى Excel/PDF
- ✅ **نظام مستخدمين**: كل مستخدم له بياناته الخاصة
- ✅ **واجهة عربية**: مصممة بالكامل باللغة العربية RTL

## 🛠️ التقنيات المستخدمة

- **Frontend**: Next.js 14 + TypeScript + Tailwind CSS
- **UI Components**: ShadCN UI + Radix UI
- **Backend**: Firebase (Firestore + Auth + Storage)
- **Charts**: Recharts
- **Forms**: React Hook Form + Zod
- **Tables**: TanStack Table

## 📋 متطلبات التشغيل

- Node.js 18+ 
- npm أو yarn
- حساب Firebase (مجاني)

## ⚙️ خطوات الإعداد

### 1️⃣ إعداد Firebase

1. اذهب إلى [Firebase Console](https://console.firebase.google.com)
2. أنشئ مشروع جديد (New Project)
3. فعّل الخدمات التالية:
   - **Authentication**: 
     - اذهب إلى Authentication > Sign-in method
     - فعّل Email/Password
   - **Firestore Database**:
     - اذهب إلى Firestore Database
     - أنشئ قاعدة بيانات في Production mode
     - اختر أقرب منطقة (مثل europe-west3)
   - **Storage**:
     - اذهب إلى Storage
     - أنشئ bucket جديد

4. احصل على بيانات الإعداد:
   - اذهب إلى Project Settings (الإعدادات)
   - في تبويب General، انزل لـ "Your apps"
   - اضغط على Web (</>) 
   - سجل التطبيق واحصل على firebaseConfig

### 2️⃣ إعداد المشروع

1. **استنسخ المشروع أو انقل الملفات**
```bash
# إذا كان على GitHub
git clone [رابط المشروع]
cd factory-flow

# أو انسخ الملفات مباشرة
```

2. **ثبت المكتبات**
```bash
npm install
# أو
yarn install
```

3. **أنشئ ملف البيئة .env.local**
```bash
# في مجلد المشروع الرئيسي، أنشئ ملف .env.local
```

أضف البيانات التالية (استبدلها ببياناتك من Firebase):
```env
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-auth-domain
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-storage-bucket
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
```

### 3️⃣ إعداد قواعد Firestore

اذهب إلى Firestore > Rules وأضف:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // المستخدمون يمكنهم الوصول لبياناتهم فقط
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### 4️⃣ إعداد قواعد Storage

اذهب إلى Storage > Rules وأضف:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

## 🚀 تشغيل المشروع

### للتطوير المحلي:
```bash
npm run dev
# أو
yarn dev
```
افتح المتصفح على: http://localhost:3000

### للإنتاج (Production):
```bash
npm run build
npm run start
```

## 🌐 النشر على الإنترنت

### الخيار 1: Vercel (مجاني وسهل)

1. أنشئ حساب على [Vercel](https://vercel.com)
2. اربط حسابك بـ GitHub
3. ارفع المشروع على GitHub
4. في Vercel، اضغط "New Project"
5. اختر المشروع من GitHub
6. أضف متغيرات البيئة (Environment Variables)
7. اضغط Deploy

### الخيار 2: Firebase Hosting

```bash
# ثبت Firebase CLI
npm install -g firebase-tools

# سجل دخول
firebase login

# ابدأ المشروع
firebase init hosting

# انشر
npm run build
firebase deploy --only hosting
```

## 👥 المشاركة مع الشركاء

لمشاركة النظام مع شركائك:

1. **نفس البيانات للجميع**:
   - كل الشركاء يستخدمون نفس مشروع Firebase
   - كل شريك ينشئ حساب بإيميله
   - البيانات مشتركة تلقائياً

2. **بيانات منفصلة لكل شريك**:
   - كل شريك ينشئ حساب منفصل
   - البيانات محفوظة بشكل منفصل لكل مستخدم
   - يمكن إضافة نظام صلاحيات لاحقاً

3. **الوصول للنظام**:
   - شارك رابط الموقع بعد النشر
   - كل شريك يسجل دخول بحسابه

## 📱 الاستخدام

### إنشاء حساب جديد:
1. افتح الموقع
2. اضغط "إنشاء حساب"
3. أدخل البريد الإلكتروني وكلمة المرور
4. ابدأ استخدام النظام

### الوظائف الأساسية:
- **العملاء**: أضف عميل جديد من صفحة العملاء
- **الفواتير**: سجل الحركات المالية من دفتر الأستاذ
- **الشيكات**: ارفع صور الشيكات وتابع حالتها
- **المخزون**: سجل دخول وخروج المواد
- **التقارير**: صدّر البيانات من أي صفحة

## 🔧 الصيانة والنسخ الاحتياطي

### النسخ الاحتياطي التلقائي:
- Firebase يحفظ نسخ احتياطية تلقائياً
- يمكنك تصدير البيانات يدوياً من Firebase Console

### التحديثات:
```bash
# تحديث المكتبات
npm update

# إعادة البناء
npm run build
```

## 📞 الدعم والمساعدة

في حالة واجهت أي مشاكل:

1. تأكد من إعدادات Firebase صحيحة
2. راجع Console في المتصفح للأخطاء
3. تأكد من قواعد Firestore و Storage

## 🔐 الأمان

- كل المستخدمين محميون بـ Authentication
- البيانات مشفرة في Firebase
- كل مستخدم يرى بياناته فقط
- الملفات محمية في Storage

## 📈 التطوير المستقبلي

خطط للتحسينات القادمة:
- [ ] نظام صلاحيات متقدم
- [ ] تطبيق موبايل
- [ ] ربط مع الأنظمة المحاسبية
- [ ] تقارير متقدمة بـ AI
- [ ] نظام إشعارات
- [ ] Barcode scanning للمخزون

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الخاص والتجاري.

---

**تم البناء بـ ❤️ لتسهيل إدارة المصانع**
